<main>


<h1>Life in the 38th State</h1>

{{#each blog}}
  <div class="container">
    <h2>{{this.title}}</h2>
    <p>{{this.content}}</p>

    <img class="image" src="{{this.image}}" alt="" />
    <p>{{this.username}}</p>
    </div>
  {{/each}}

</main>
